<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="compsci.travelers">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Travelers"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="compsci.travelers.presentation.MainActivity"
            android:label="Travelers"></activity>
        <activity
            android:name="compsci.travelers.presentation.FlightList"
            android:parentActivityName="compsci.travelers.presentation.FlightForm" />
        <activity
            android:name="compsci.travelers.presentation.FlightForm"
            android:parentActivityName="compsci.travelers.presentation.MainActivity" />
        <activity
            android:name="compsci.travelers.presentation.Summary"
            android:parentActivityName="compsci.travelers.presentation.FlightList" />
        <activity
            android:name="compsci.travelers.presentation.Payment"
            android:parentActivityName="compsci.travelers.presentation.Summary"></activity>
        <activity
            android:name="compsci.travelers.presentation.ThankYou"
            android:parentActivityName="compsci.travelers.presentation.Payment"></activity>

        <activity
            android:name="compsci.travelers.presentation.SplashActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>